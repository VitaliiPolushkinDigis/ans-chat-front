export enum processList {
  count = 'count',
  getData = 'getData',
}

export enum ProfileEnum {
  userId = 'User Id',
  id = 'ID',
  FullName = 'Full Name',
  url = 'Url',
  thumbnailUrl = 'Thumbnail',
}

export enum Period {
  initial = 'initial',
  next = 'next',
  prev = 'prev',
  pageNumber = 'pageNumber',
}

export enum Sex {
  male = 'male',
  female = 'female',
}
