export enum processList {
  count = 'count',
  getData = 'getData',
}

export enum ProfileEnum {
  userId = 'User Id',
  id = 'ID',
  FullName = 'Full Name',
  url = 'Url',
  thumbnailUrl = 'Thumbnail',
}
